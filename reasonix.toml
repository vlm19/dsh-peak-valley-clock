[permissions]
allow = ["Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\nfunction partsInZone(instant, timezone) {\n  const fmt = new Intl.DateTimeFormat(\"en-CA\", {\n    timeZone: timezone, hour12: false,\n    year: \"numeric\", month: \"2-digit\", day: \"2-digit\",\n    hour: \"2-digit\", minute: \"2-digit\", weekday: \"short\",\n  })\n  const parts = fmt.formatToParts(instant)\n  const lookup = (t) => parts.find(p => p.type === t)?.value ?? \"\"\n  return { year: Number(lookup(\"year\")), month: Number(lookup(\"month\")), day: Number(lookup(\"day\")),\n    hour: Number(lookup(\"hour\")), minute: Number(lookup(\"minute\")), weekday: lookup(\"weekday\") }\n}\n// 上海 10:00 = UTC 02:00\nconsole.log(\"UTC 2026-08-18T02:00Z -> Shanghai:\", partsInZone(new Date(\"2026-08-18T02:00:00Z\"), \"Asia/Shanghai\"))\n// 上海 09:00 = UTC 01:00\nconsole.log(\"UTC 2026-08-18T01:00Z -> Shanghai:\", partsInZone(new Date(\"2026-08-18T01:00:00Z\"), \"Asia/Shanghai\"))\n// 上海 00:00 = UTC 16:00 prev day\nconsole.log(\"UTC 2026-08-17T16:00Z -> Shanghai:\", partsInZone(new Date(\"2026-08-17T16:00:00Z\"), \"Asia/Shanghai\"))\n'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\nfunction parseHHMM(value) {\n  const match = /^(\\d{1,2}):(\\d{2})$/.exec(value)\n  const h = Number(match[1]), m = Number(match[2])\n  return [h, m]\n}\nfunction partsInZone(instant, timezone) {\n  const fmt = new Intl.DateTimeFormat(\"en-CA\", { timeZone: timezone, hour12: false,\n    year: \"numeric\", month: \"2-digit\", day: \"2-digit\", hour: \"2-digit\", minute: \"2-digit\", weekday: \"short\" })\n  const parts = fmt.formatToParts(instant)\n  const lookup = (t) => parts.find(p => p.type === t)?.value ?? \"\"\n  const weekdayMap = { Sun:0, Mon:1, Tue:2, Wed:3, Thu:4, Fri:5, Sat:6 }\n  let hour = Number(lookup(\"hour\")); if (hour === 24) hour = 0\n  return { year: Number(lookup(\"year\")), month: Number(lookup(\"month\")), day: Number(lookup(\"day\")),\n    hour, minute: Number(lookup(\"minute\")), dayOfWeek: weekdayMap[lookup(\"weekday\")] ?? 0 }\n}\nfunction tzOffsetMinutes(instant, timezone) {\n  const parts = new Intl.DateTimeFormat(\"en-US\", { timeZone: timezone, hour12: false,\n    year: \"numeric\", month: \"2-digit\", day: \"2-digit\", hour: \"2-digit\", minute: \"2-digit\", second: \"2-digit\" }).formatToParts(instant)\n  const lookup = (t) => Number(parts.find(p => p.type === t)?.value ?? 0)\n  const asUtc = Date.UTC(lookup(\"year\"), lookup(\"month\")-1, lookup(\"day\"), lookup(\"hour\"), lookup(\"minute\"), lookup(\"second\"))\n  return Math.round((asUtc - instant.getTime()) / 60000)\n}\nfunction instantFromZone(year, month, day, hour, minute, timezone) {\n  const guess = new Date(Date.UTC(year, month-1, day, hour, minute, 0, 0))\n  const offsetMin = tzOffsetMinutes(guess, timezone)\n  return new Date(guess.getTime() - offsetMin * 60000)\n}\nfunction rollDay(year, month, day) {\n  const d = new Date(Date.UTC(year, month-1, day))\n  return { year: d.getUTCFullYear(), month: d.getUTCMonth()+1, day: d.getUTCDate() }\n}\nfunction invertTier(t) { return t === \"peak\" ? \"offpeak\" : \"peak\" }\nfunction computeNextBoundary(now, schedule) {\n  const tz = schedule.timezone\n  const nowParts = partsInZone(now, tz)\n  const nowMin = nowParts.hour * 60 + nowParts.minute\n  const todays = []\n  for (const w of schedule.windows) {\n    const [sh, sm] = parseHHMM(w.start)\n    const [eh, em] = parseHHMM(w.end)\n    const startMin = sh*60+sm, endMin = eh*60+em\n    todays.push({ dayOffset: 0, min: startMin, tier: w.tier })\n    if (endMin > startMin) todays.push({ dayOffset: 0, min: endMin, tier: invertTier(w.tier) })\n    else { todays.push({ dayOffset: 0, min: endMin, tier: invertTier(w.tier) }); todays.push({ dayOffset: 1, min: 24*60, tier: w.tier }) }\n  }\n  let best\n  for (const b of todays) {\n    if (b.dayOffset === 0 && b.min <= nowMin) continue\n    if (best === undefined || b.dayOffset < best.dayOffset || (b.dayOffset === best.dayOffset && b.min < best.min)) best = b\n  }\n  const targetParts = nowParts\n  let targetYear = targetParts.year, targetMonth = targetParts.month, targetDay = targetParts.day + (best?.dayOffset ?? 0)\n  const rolled = rollDay(targetYear, targetMonth, targetDay)\n  targetYear = rolled.year; targetMonth = rolled.month; targetDay = rolled.day\n  const min = best?.min ?? 0\n  return instantFromZone(targetYear, targetMonth, targetDay, Math.floor(min/60), min%60, tz)\n}\nconst SCHEDULE = { timezone: \"Asia/Shanghai\", windows: [\n  { start: \"09:00\", end: \"12:00\", tier: \"peak\" },\n  { start: \"14:00\", end: \"18:00\", tier: \"peak\" },\n] }\nfunction shaTime(y,mo,d,h,mi){ return new Date(Date.UTC(y,mo-1,d,h-8,mi,0)) }\nfor (const [label, now] of [\n  [\"09:30\", shaTime(2026,8,18,9,30)],\n  [\"13:00\", shaTime(2026,8,18,13,0)],\n  [\"23:00\", shaTime(2026,8,18,23,0)],\n  [\"00:30\", shaTime(2026,8,18,0,30)],\n  [\"19:00\", shaTime(2026,8,18,19,0)],\n]) {\n  const nb = computeNextBoundary(now, SCHEDULE)\n  const p = partsInZone(nb, \"Asia/Shanghai\")\n  console.log(label, \"-> next boundary Shanghai:\", `${p.year}-${p.month}-${p.day} ${p.hour}:${String(p.minute).padStart(2,\"0\")}`, \"msRemaining:\", nb.getTime() - now.getTime())\n}\n'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\n// 模拟：用户本机时区 = America/Los_Angeles，把“本机钟面时间”调到上午 10:00（用户以为进入峰时段）\n// 洛杉矶 2026-08-18 10:00 (PDT, UTC-7) = UTC 2026-08-18 17:00\nconst userLocalTenAm = new Date(\"2026-08-18T17:00:00Z\")\nfunction partsInZone(instant, timezone) {\n  const fmt = new Intl.DateTimeFormat(\"en-CA\", { timeZone: timezone, hour12: false,\n    year:\"numeric\",month:\"2-digit\",day:\"2-digit\",hour:\"2-digit\",minute:\"2-digit\" })\n  const parts = fmt.formatToParts(instant)\n  const l = t => parts.find(p=>p.type===t)?.value ?? \"\"\n  return `${l(\"year\")}-${l(\"month\")}-${l(\"day\")} ${l(\"hour\")}:${l(\"minute\")}`\n}\nconsole.log(\"用户本机钟面(洛杉矶):\", partsInZone(userLocalTenAm, \"America/Los_Angeles\"))\nconsole.log(\"插件按 Asia/Shanghai 判定:\", partsInZone(userLocalTenAm, \"Asia/Shanghai\"), \"(=> 谷，不是峰)\")\n'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\nfunction parseHHMM(value){const m=/^(\\d{1,2}):(\\d{2})$/.exec(value);const h=Number(m[1]),mi=Number(m[2]);return[h,mi]}\nfunction partsInZone(instant,timezone){const fmt=new Intl.DateTimeFormat(\"en-CA\",{timeZone:timezone,hour12:false,year:\"numeric\",month:\"2-digit\",day:\"2-digit\",hour:\"2-digit\",minute:\"2-digit\"});const parts=fmt.formatToParts(instant);const l=t=>parts.find(p=>p.type===t)?.value??\"\";let hour=Number(l(\"hour\"));if(hour===24)hour=0;return{year:Number(l(\"year\")),month:Number(l(\"month\")),day:Number(l(\"day\")),hour,minute:Number(l(\"minute\"))}}\nfunction tzOffsetMinutes(instant,timezone){const parts=new Intl.DateTimeFormat(\"en-US\",{timeZone:timezone,hour12:false,year:\"numeric\",month:\"2-digit\",day:\"2-digit\",hour:\"2-digit\",minute:\"2-digit\",second:\"2-digit\"}).formatToParts(instant);const l=t=>Number(parts.find(p=>p.type===t)?.value??0);const asUtc=Date.UTC(l(\"year\"),l(\"month\")-1,l(\"day\"),l(\"hour\"),l(\"minute\"),l(\"second\"));return Math.round((asUtc-instant.getTime())/60000)}\nfunction instantFromZone(year,month,day,hour,minute,timezone){const guess=new Date(Date.UTC(year,month-1,day,hour,minute,0,0));const off=tzOffsetMinutes(guess,timezone);return new Date(guess.getTime()-off*60000)}\nfunction rollDay(year,month,day){const d=new Date(Date.UTC(year,month-1,day));return{year:d.getUTCFullYear(),month:d.getUTCMonth()+1,day:d.getUTCDate()}}\nfunction invertTier(t){return t===\"peak\"?\"offpeak\":\"peak\"}\nfunction computeNextBoundary(now,schedule){const tz=schedule.timezone;const nowParts=partsInZone(now,tz);const nowMin=nowParts.hour*60+nowParts.minute;const cands=[];for(let day=-1;day<=1;day++){for(const w of schedule.windows){const[sh,sm]=parseHHMM(w.start);const[eh,em]=parseHHMM(w.end);const startMin=sh*60+sm;const endMin=eh*60+em;cands.push({dayOffset:day,min:startMin,tier:w.tier});cands.push({dayOffset:endMin>startMin?day:day+1,min:endMin,tier:invertTier(w.tier)})}}let best;for(const b of cands){if(b.dayOffset>0||(b.dayOffset===0&&b.min>nowMin)){if(best===undefined||b.dayOffset<best.dayOffset||(b.dayOffset===best.dayOffset&&b.min<best.min))best=b}}if(best===undefined)return new Date(now.getTime()+86400000);const rolled=rollDay(nowParts.year,nowParts.month,nowParts.day+best.dayOffset);return instantFromZone(rolled.year,rolled.month,rolled.day,Math.floor(best.min/60),best.min%60,tz)}\nconst SCHEDULE={timezone:\"Asia/Shanghai\",windows:[{start:\"09:00\",end:\"12:00\",tier:\"peak\"},{start:\"14:00\",end:\"18:00\",tier:\"peak\"}]}\nconst CROSS={timezone:\"Asia/Shanghai\",windows:[{start:\"23:00\",end:\"01:00\",tier:\"peak\"}]}\nfunction shaTime(y,mo,d,h,mi){return new Date(Date.UTC(y,mo-1,d,h-8,mi,0))}\nfunction show(label,now,sched){const nb=computeNextBoundary(now,sched);const p=partsInZone(nb,\"Asia/Shanghai\");const ms=nb.getTime()-now.getTime();console.log(label.padEnd(28),\"->\",`${p.year}-${p.month}-${p.day} ${String(p.hour).padStart(2,\"0\")}:${String(p.minute).padStart(2,\"0\")}`,\"  ms:\",ms)}\nconsole.log(\"== 默认 schedule (09-12 / 14-18 峰) ==\")\nshow(\"09:30\",shaTime(2026,8,18,9,30),SCHEDULE)\nshow(\"13:00\",shaTime(2026,8,18,13,0),SCHEDULE)\nshow(\"18:30\",shaTime(2026,8,18,18,30),SCHEDULE)\nshow(\"19:00\",shaTime(2026,8,18,19,0),SCHEDULE)\nshow(\"23:00\",shaTime(2026,8,18,23,0),SCHEDULE)\nshow(\"00:30\",shaTime(2026,8,18,0,30),SCHEDULE)\nconsole.log(\"== 跨午夜 schedule (23:00-01:00 峰) ==\")\nshow(\"23:30\",shaTime(2026,8,18,23,30),CROSS)\nshow(\"00:30\",shaTime(2026,8,18,0,30),CROSS)\nshow(\"12:00\",shaTime(2026,8,18,12,0),CROSS)\n'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\nconst m = await import(\"./src/tariff.ts\")\nconsole.log(\"exports:\", Object.keys(m).slice(0, 20).join(\", \"))\nconst t = m.resolveTier(new Date(\"2026-08-18T01:30:00Z\"), m.DEFAULT_SCHEDULE)\nconsole.log(\"resolveTier(上海09:30).tier =\", t.tier)\n'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\nimport { DEFAULT_SCHEDULE, computeNextBoundary, partsInZone, resolveTier } from \"./src/tariff.ts\"\nconst shaTime = (y, mo, d, h, mi) => new Date(Date.UTC(y, mo - 1, d, h - 8, mi, 0))\nconst p = (dt) => { const x = partsInZone(dt, \"Asia/Shanghai\"); return `${x.year}-${x.month}-${x.day} ${x.hour}:${String(x.minute).padStart(2, \"0\")}` }\nlet pass = 0, fail = 0\nfunction check(label, actual, expected) {\n  const ok = actual === expected\n  if (ok) pass++; else { fail++; console.log(\"FAIL\", label, \"got\", actual, \"want\", expected) }\n  return ok\n}\n// resolveTier 边界\ncheck(\"08:59 tier\", resolveTier(shaTime(2026,8,18,8,59), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"09:00 tier\", resolveTier(shaTime(2026,8,18,9,0), DEFAULT_SCHEDULE).tier, \"peak\")\ncheck(\"12:00 tier\", resolveTier(shaTime(2026,8,18,12,0), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"14:00 tier\", resolveTier(shaTime(2026,8,18,14,0), DEFAULT_SCHEDULE).tier, \"peak\")\ncheck(\"18:00 tier\", resolveTier(shaTime(2026,8,18,18,0), DEFAULT_SCHEDULE).tier, \"offpeak\")\n// computeNextBoundary 默认 schedule\ncheck(\"09:30 ->12:00\", p(computeNextBoundary(shaTime(2026,8,18,9,30), DEFAULT_SCHEDULE)), \"2026-8-18 12:0\")\ncheck(\"13:00 ->14:00\", p(computeNextBoundary(shaTime(2026,8,18,13,0), DEFAULT_SCHEDULE)), \"2026-8-18 14:0\")\ncheck(\"23:00 ->次日09:00\", p(computeNextBoundary(shaTime(2026,8,18,23,0), DEFAULT_SCHEDULE)), \"2026-8-19 9:0\")\ncheck(\"00:30 ->09:00\", p(computeNextBoundary(shaTime(2026,8,18,0,30), DEFAULT_SCHEDULE)), \"2026-8-18 9:0\")\ncheck(\"19:00 ->次日09:00\", p(computeNextBoundary(shaTime(2026,8,18,19,0), DEFAULT_SCHEDULE)), \"2026-8-19 9:0\")\n// 跨午夜\nconst CROSS = { timezone: \"Asia/Shanghai\", windows: [{ start: \"23:00\", end: \"01:00\", tier: \"peak\" }], prices: [] }\ncheck(\"23:30 ->次日01:00\", p(computeNextBoundary(shaTime(2026,8,18,23,30), CROSS)), \"2026-8-19 1:0\")\ncheck(\"00:30 ->当天01:00\", p(computeNextBoundary(shaTime(2026,8,18,0,30), CROSS)), \"2026-8-18 1:0\")\nconsole.log(`\\n${pass} passed, ${fail} failed`)\nprocess.exit(fail === 0 ? 0 : 1)\n'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\nimport { DEFAULT_SCHEDULE, computeNextBoundary, partsInZone, resolveTier } from \"./src/tariff.ts\"\nconst shaTime = (y, mo, d, h, mi) => new Date(Date.UTC(y, mo - 1, d, h - 8, mi, 0))\nconst p = (dt) => { const x = partsInZone(dt, \"Asia/Shanghai\"); return `${x.year}-${x.month}-${x.day} ${x.hour}:${String(x.minute).padStart(2, \"0\")}` }\nlet pass = 0, fail = 0\nfunction check(label, actual, expected) {\n  const ok = actual === expected\n  if (ok) pass++; else { fail++; console.log(\"FAIL\", label, \"got\", JSON.stringify(actual), \"want\", JSON.stringify(expected)) }\n}\ncheck(\"08:59 tier\", resolveTier(shaTime(2026,8,18,8,59), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"09:00 tier\", resolveTier(shaTime(2026,8,18,9,0), DEFAULT_SCHEDULE).tier, \"peak\")\ncheck(\"12:00 tier\", resolveTier(shaTime(2026,8,18,12,0), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"14:00 tier\", resolveTier(shaTime(2026,8,18,14,0), DEFAULT_SCHEDULE).tier, \"peak\")\ncheck(\"18:00 tier\", resolveTier(shaTime(2026,8,18,18,0), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"09:30 ->12:00\", p(computeNextBoundary(shaTime(2026,8,18,9,30), DEFAULT_SCHEDULE)), \"2026-8-18 12:00\")\ncheck(\"13:00 ->14:00\", p(computeNextBoundary(shaTime(2026,8,18,13,0), DEFAULT_SCHEDULE)), \"2026-8-18 14:00\")\ncheck(\"23:00 ->次日09:00\", p(computeNextBoundary(shaTime(2026,8,18,23,0), DEFAULT_SCHEDULE)), \"2026-8-19 09:00\")\ncheck(\"00:30 ->09:00\", p(computeNextBoundary(shaTime(2026,8,18,0,30), DEFAULT_SCHEDULE)), \"2026-8-18 09:00\")\ncheck(\"19:00 ->次日09:00\", p(computeNextBoundary(shaTime(2026,8,18,19,0), DEFAULT_SCHEDULE)), \"2026-8-19 09:00\")\nconst CROSS = { timezone: \"Asia/Shanghai\", windows: [{ start: \"23:00\", end: \"01:00\", tier: \"peak\" }], prices: [] }\ncheck(\"23:30 ->次日01:00\", p(computeNextBoundary(shaTime(2026,8,18,23,30), CROSS)), \"2026-8-19 01:00\")\ncheck(\"00:30 ->当天01:00\", p(computeNextBoundary(shaTime(2026,8,18,0,30), CROSS)), \"2026-8-18 01:00\")\nconsole.log(`${pass} passed, ${fail} failed`)\nprocess.exit(fail === 0 ? 0 : 1)\n'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\nimport { DEFAULT_SCHEDULE, computeNextBoundary, partsInZone, resolveTier } from \"./src/tariff.ts\"\nconst shaTime = (y, mo, d, h, mi) => new Date(Date.UTC(y, mo - 1, d, h - 8, mi, 0))\nconst p = (dt) => { const x = partsInZone(dt, \"Asia/Shanghai\"); return `${x.year}-${x.month}-${x.day} ${String(x.hour).padStart(2, \"0\")}:${String(x.minute).padStart(2, \"0\")}` }\nlet pass = 0, fail = 0\nfunction check(label, actual, expected) {\n  const ok = actual === expected\n  if (ok) pass++; else { fail++; console.log(\"FAIL\", label, \"got\", JSON.stringify(actual), \"want\", JSON.stringify(expected)) }\n}\ncheck(\"08:59 tier\", resolveTier(shaTime(2026,8,18,8,59), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"09:00 tier\", resolveTier(shaTime(2026,8,18,9,0), DEFAULT_SCHEDULE).tier, \"peak\")\ncheck(\"12:00 tier\", resolveTier(shaTime(2026,8,18,12,0), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"14:00 tier\", resolveTier(shaTime(2026,8,18,14,0), DEFAULT_SCHEDULE).tier, \"peak\")\ncheck(\"18:00 tier\", resolveTier(shaTime(2026,8,18,18,0), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"09:30 ->12:00\", p(computeNextBoundary(shaTime(2026,8,18,9,30), DEFAULT_SCHEDULE)), \"2026-8-18 12:00\")\ncheck(\"13:00 ->14:00\", p(computeNextBoundary(shaTime(2026,8,18,13,0), DEFAULT_SCHEDULE)), \"2026-8-18 14:00\")\ncheck(\"23:00 ->次日09:00\", p(computeNextBoundary(shaTime(2026,8,18,23,0), DEFAULT_SCHEDULE)), \"2026-8-19 09:00\")\ncheck(\"00:30 ->09:00\", p(computeNextBoundary(shaTime(2026,8,18,0,30), DEFAULT_SCHEDULE)), \"2026-8-18 09:00\")\ncheck(\"19:00 ->次日09:00\", p(computeNextBoundary(shaTime(2026,8,18,19,0), DEFAULT_SCHEDULE)), \"2026-8-19 09:00\")\nconst CROSS = { timezone: \"Asia/Shanghai\", windows: [{ start: \"23:00\", end: \"01:00\", tier: \"peak\" }], prices: [] }\ncheck(\"23:30 ->次日01:00\", p(computeNextBoundary(shaTime(2026,8,18,23,30), CROSS)), \"2026-8-19 01:00\")\ncheck(\"00:30 ->当天01:00\", p(computeNextBoundary(shaTime(2026,8,18,0,30), CROSS)), \"2026-8-18 01:00\")\nconsole.log(`${pass} passed, ${fail} failed`)\nprocess.exit(fail === 0 ? 0 : 1)\n'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && ls -d node_modules 2>/dev/null; echo \"node_modules exists: $?\"; command -v tsc; command -v vitest; command -v bun; command -v tsx; echo \"done\"", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e 'import {computeNextBoundary,partsInZone,DEFAULT_SCHEDULE} from \"./src/tariff.ts\"; const now=new Date(\"2026-08-18T11:00:00Z\"); const nb=computeNextBoundary(now,DEFAULT_SCHEDULE); const p=partsInZone(nb,\"Asia/Shanghai\"); console.log(`next=${p.day} ${p.hour}:${p.minute}`)'", "Bash=cd \"D:\\GitHub\\dsh-peak-valley-clock\" && node --input-type=module -e '\nimport { DEFAULT_SCHEDULE, computeNextBoundary, partsInZone, resolveTier } from \"./src/tariff.ts\"\nconst shaTime = (y, mo, d, h, mi) => new Date(Date.UTC(y, mo - 1, d, h - 8, mi, 0))\nconst p = (dt) => { const x = partsInZone(dt, \"Asia/Shanghai\"); return `${x.year}-${x.month}-${x.day} ${String(x.hour).padStart(2, \"0\")}:${String(x.minute).padStart(2, \"0\")}` }\nlet pass = 0, fail = 0\nfunction check(label, actual, expected) {\n  if (actual === expected) pass++; else { fail++; console.log(\"FAIL\", label, \"got\", JSON.stringify(actual), \"want\", JSON.stringify(expected)) }\n}\ncheck(\"08:59 tier\", resolveTier(shaTime(2026,8,18,8,59), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"09:00 tier\", resolveTier(shaTime(2026,8,18,9,0), DEFAULT_SCHEDULE).tier, \"peak\")\ncheck(\"12:00 tier\", resolveTier(shaTime(2026,8,18,12,0), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"14:00 tier\", resolveTier(shaTime(2026,8,18,14,0), DEFAULT_SCHEDULE).tier, \"peak\")\ncheck(\"18:00 tier\", resolveTier(shaTime(2026,8,18,18,0), DEFAULT_SCHEDULE).tier, \"offpeak\")\ncheck(\"09:30 ->12:00\", p(computeNextBoundary(shaTime(2026,8,18,9,30), DEFAULT_SCHEDULE)), \"2026-8-18 12:00\")\ncheck(\"13:00 ->14:00\", p(computeNextBoundary(shaTime(2026,8,18,13,0), DEFAULT_SCHEDULE)), \"2026-8-18 14:00\")\ncheck(\"23:00 ->次日09:00\", p(computeNextBoundary(shaTime(2026,8,18,23,0), DEFAULT_SCHEDULE)), \"2026-8-19 09:00\")\ncheck(\"00:30 ->09:00\", p(computeNextBoundary(shaTime(2026,8,18,0,30), DEFAULT_SCHEDULE)), \"2026-8-18 09:00\")\ncheck(\"19:00 ->次日09:00\", p(computeNextBoundary(shaTime(2026,8,18,19,0), DEFAULT_SCHEDULE)), \"2026-8-19 09:00\")\nconst CROSS = { timezone: \"Asia/Shanghai\", windows: [{ start: \"23:00\", end: \"01:00\", tier: \"peak\" }], prices: [] }\ncheck(\"23:30 ->次日01:00\", p(computeNextBoundary(shaTime(2026,8,18,23,30), CROSS)), \"2026-8-19 01:00\")\ncheck(\"00:30 ->当天01:00\", p(computeNextBoundary(shaTime(2026,8,18,0,30), CROSS)), \"2026-8-18 01:00\")\nconsole.log(`${pass} passed, ${fail} failed`)\nprocess.exit(fail === 0 ? 0 : 1)\n'"]
